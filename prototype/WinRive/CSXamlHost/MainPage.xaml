<Page
    x:Class="CSXamlHost.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CSXamlHost"
    xmlns:controls="using:CSXamlHost.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="20,10,20,10"
            HorizontalAlignment="Center"
            Style="{StaticResource HeaderTextBlockStyle}"
            Text=".NET9 UWP XAML Rive Animation Demo - Refactored Architecture" />

        <!-- Rive Viewer Control -->
        <controls:RiveViewerControl
            x:Name="RiveViewer"
            Grid.Row="1"
            Grid.Column="0"
            Margin="20"
            AutoPlay="True"
            ShowStatusBar="True"
            ShowFpsCounter="False"
            PlaceholderText="Select a Rive file to begin..." />

        <!-- State Machine Panel -->
        <controls:RiveStateMachinePanel
            x:Name="StateMachinePanel"
            Grid.Row="1"
            Grid.Column="1"
            Margin="10"
            RiveViewer="{x:Bind RiveViewer}"
            AllowExternalFiles="True"
            ShowStatus="True" />
    </Grid>
</Page>
