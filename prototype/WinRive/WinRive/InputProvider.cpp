#include "pch.h"
#include "InputProvider.h"

namespace winrt::WinRive::implementation
{
    // NullInputProvider implementation is header-only (inline),
    // so no implementation needed here for now.
    
    // This file can be extended in the future if the abstract
    // interface needs any shared implementation details.
}
